#################################################
# Host Configurations
#
# ansible-playbook -l host -i baremetal_hosts -u $(whoami) default.host.yml -K
#################################################
---
- hosts: host
  become: yes
  vars_files:
    - group_vars/baremetal_vars
  vars:
    network_retries: 5
    network_delay: 20
    command_retries: 3
    command_delay: 10

  pre_tasks:
    - name: Gather installed package facts
      package_facts:
        manager: auto

    - name: Detect Ubuntu desktop host
      set_fact:
        xws_is_ubuntu_desktop_host: >-
          {{
            ansible_facts['distribution'] == 'Ubuntu' and (
              'ubuntu-desktop' in ansible_facts.packages or
              'ubuntu-desktop-minimal' in ansible_facts.packages or
              'gdm3' in ansible_facts.packages
            )
          }}

  tasks:
    - name: Install virtualbox if needed
      become: true
      apt:
        name: virtualbox
        update_cache: yes
        cache_valid_time: 3600
        state: present
      register: virtualbox_install
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: virtualbox_install is succeeded

    - name: Remove legacy fuse package (prevents GDM breakage on Ubuntu desktop)
      become: true
      apt:
        name: fuse
        state: absent
        purge: yes
      when: ansible_facts['distribution'] == 'Ubuntu'

    - name: Install FUSE3 runtime
      become: true
      apt:
        name:
          - fuse3
          - libfuse3-3
        update_cache: yes
        cache_valid_time: 3600
        state: present
      register: fuse3_install
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: fuse3_install is succeeded

    # If a previous run installed legacy fuse, ensure desktop packages are healthy.
    - name: Repair Ubuntu desktop packages after fuse migration
      become: true
      command: apt-get install --reinstall -y ubuntu-desktop gdm3
      when:
        - xws_is_ubuntu_desktop_host
        - "'fuse' in ansible_facts.packages"

    - name: Install headers
      become: true
      apt:
        name: linux-headers-generic
        state: present
      register: headers_install
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: headers_install is succeeded

    - name: Install virtualbox-dkms if needed
      become: true
      apt:
        name: virtualbox-dkms
        update_cache: yes
        cache_valid_time: 3600
        state: present
      register: virtualbox_dkms_install
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: virtualbox_dkms_install is succeeded

    - name: Reconfigure virtualbox-dkms (allow non-running kernel failures)
      command: dpkg-reconfigure virtualbox-dkms
      become: true
      register: vbox_dkms_reconfigure
      failed_when: false
      args:
        chdir: /{{ playbook_dir }}/vagrant/

    - debug: var=vbox_dkms_reconfigure.stdout_lines

    - name: Check VirtualBox DKMS status
      command: dkms status virtualbox
      become: true
      register: vbox_dkms_status
      changed_when: false

    - name: Ensure VirtualBox DKMS is available for the running kernel
      assert:
        that:
          - >-
            (vbox_dkms_status.stdout_lines
            | select('search', ', ' ~ ansible_facts['kernel'] ~ ', .*(installed|built)')
            | list
            | length) > 0
        fail_msg: >-
          virtualbox-dkms was not built for running kernel {{ ansible_facts['kernel'] }}.
          See /var/lib/dkms/virtualbox/7.0.16/build/make.log and task output for details.

    - name: dpkg-reconfigure virtualbox 
      command: dpkg-reconfigure virtualbox
      become: true
      register: out
      args:
        chdir: /{{ playbook_dir }}/vagrant/

    - debug: var=out.stdout_lines


    - name: Install git
      become: true
      apt:
        name: git
        state: present
      register: git_install
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: git_install is succeeded

    - local_action: stat path=/{{ playbook_dir }}/vagrant/{{ vagrant_zip_file }}
      register: zipped_vagrant_exists
      tags: vagrant

    - name: Install sshfs
      become: true
      apt:
        name: sshfs
        state: present
      register: sshfs_install
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: sshfs_install is succeeded

    - local_action: stat path=/{{ playbook_dir }}/vagrant/{{ vagrant_zip_file }}
      register: zipped_vagrant_exists
      tags: vagrant

    # @todo: Replace method above by using apt-get:
    # sudo apt update
    # sudo apt -y install apt-transport-https ca-certificates curl software-properties-common
    # wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg
    # echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
    # sudo apt update
    # sudo apt install vagrant

    - name: If vagrant is not available get vagrant zip source and checksum it
      get_url:
        url: "{{ vagrant_zip_base_path }}{{ vagrant_zip_file }}"
        dest: /{{ playbook_dir }}/vagrant/{{ vagrant_zip_file }}
        checksum: "{{ vagrant_zip_file_checksum }}"
        force: no
      register: get_vagrant
      when: zipped_vagrant_exists.stat.exists == False
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: get_vagrant is succeeded
      tags: vagrant

    - name: Install unzip
      become: true
      apt:
        name: unzip
        update_cache: yes
        cache_valid_time: 3600
        state: present
      register: unzip_install
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: unzip_install is succeeded

    - name: Unzip vagrant zip if available and keep it
      unarchive:
        src: /{{ playbook_dir }}/vagrant/{{ vagrant_zip_file }}
        dest: /{{ playbook_dir }}/vagrant/
        remote_src: yes
        creates: /{{ playbook_dir }}/vagrant/vagrant
      tags: [vagrant, unzip]

    - name: Ensure Vagrant local state directory exists
      ansible.builtin.file:
        path: /{{ playbook_dir }}/vagrant/.vagrant/machines
        state: directory
        owner: "{{ baremetal_host_username }}"
        group: "{{ baremetal_host_username }}"
        mode: "0775"
        recurse: true
      become: true

    - name: Ensure Vagrant local state directory is writable by host user
      ansible.builtin.command: chmod -R u+rwX /{{ playbook_dir }}/vagrant/.vagrant
      become: true
      changed_when: false

    # cp id_rsa.pub from user to /root/ssh

    - name: Remove vagrant zip source
      file:
        path: /{{ playbook_dir }}/vagrant/{{ vagrant_zip_file }}
        state: absent
      tags: vagrant

    - name: ls
      shell: ls {{ playbook_dir }}/vagrant
      register: out

    - debug: var=out.stdout_lines

    - name: Validate minimum Ubuntu version for AppImage FUSE support
      assert:
        that:
          - ansible_facts['distribution_version'] is version('22.04', '>=')
        fail_msg: >-
          Ubuntu {{ ansible_facts['distribution_version'] }} is not supported. Minimum supported version is 22.04.
      when: ansible_facts['distribution'] == 'Ubuntu'

    - name: Ensure Ubuntu universe repository is enabled for AppImage FUSE packages
      become: true
      apt_repository:
        repo: "deb http://archive.ubuntu.com/ubuntu {{ ansible_facts['distribution_release'] }} universe"
        state: present
        update_cache: true
      register: ubuntu_universe_repo
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: ubuntu_universe_repo is succeeded
      when:
        - ansible_facts['distribution'] == 'Ubuntu'
        - ansible_facts['distribution_version'] is version('22.04', '>=')

    - name: Install AppImage FUSE compatibility package on Ubuntu 22.04-23.10
      become: true
      apt:
        name: libfuse2
        state: present
        update_cache: true
        cache_valid_time: 3600
      register: libfuse2_install
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: libfuse2_install is succeeded
      when:
        - ansible_facts['distribution'] == 'Ubuntu'
        - ansible_facts['distribution_version'] is version('22.04', '>=')
        - ansible_facts['distribution_version'] is version('24.04', '<')

    - name: Install AppImage FUSE compatibility package on Ubuntu 24.04+
      become: true
      apt:
        name: libfuse2t64
        state: present
        update_cache: true
        cache_valid_time: 3600
      register: libfuse2t64_install
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: libfuse2t64_install is succeeded
      when:
        - ansible_facts['distribution'] == 'Ubuntu'
        - ansible_facts['distribution_version'] is version('24.04', '>=')

    - name: install guest additions
      command: ./vagrant plugin install vagrant-vbguest
      register: out
      retries: "{{ command_retries }}"
      delay: "{{ command_delay }}"
      until: out.rc == 0
      args:
        chdir: /{{ playbook_dir }}/vagrant/

    - debug: var=out.stdout_lines

    - name: vagrant force destroy if present
      command: ./vagrant destroy -f
      register: out
      ignore_errors: true 
      args:
        chdir: /{{ playbook_dir }}/vagrant/
      become: false
      tags: vagrant-force-destroy

    - debug: var=out.stdout_lines

    - name: vagrant plugin install vagrant-disksize
      shell: "./vagrant plugin install vagrant-disksize"
      tags: disksize
      register: out
      retries: "{{ command_retries }}"
      delay: "{{ command_delay }}"
      until: out.rc == 0
      args:
        chdir: /{{ playbook_dir }}/vagrant/
      become: false
    - debug: var=out.stdout_lines

    - name: Check vagrant plugin list
      shell: "./vagrant plugin list"
      args:
        chdir: /{{ playbook_dir }}/vagrant/
      tags: disksize
      register: plugin_list_output
      retries: "{{ command_retries }}"
      delay: "{{ command_delay }}"
      until: plugin_list_output.rc == 0

    - debug:
        var: plugin_install_output.stdout

    - debug:
        var: plugin_list_output.stdout

    # @todo: ./vagrant box add {{ default_box }}

    - name: add box if needed
      shell: "./vagrant box add {{ default_box }} --insecure"
      register: out
      ignore_errors: true
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      args:
        chdir: /{{ playbook_dir }}/vagrant/
      become: false
      tags: add-box

    - debug: var=out.stdout_lines

    - local_action: stat path=/{{ playbook_dir }}/roles/geerlingguy.apache
      register: geerlingguy_apache_exists
      tags: ansible

    - name: install geerlingguy apache from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.apache
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: ansible
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_apache_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: ansible

    - local_action: stat path=/{{ playbook_dir }}/roles/geerlingguy.php
      register: geerlingguy_php_exists
      tags: [ansible, ansible_php]

    - name: install geerlingguy php from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.php
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, ansible_php]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_php_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, ansible_php]

    - local_action: stat path=/{{ playbook_dir }}/roles/geerlingguy.php-mysql
      register: geerlingguy_php_mysql_exists
      tags: [ansible, ansible_php_mysql]

    - name: install geerlingguy php-mysql from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.php-mysql
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, ansible_php_mysql]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_php_mysql_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, ansible_php_mysql]

    - local_action: stat path=/{{ playbook_dir }}/roles/geerlingguy.adminer
      register: geerlingguy_adminer_exists
      tags: [ansible, ansible_adminer]

    - name: install geerlingguy adminer from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.adminer
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, ansible_adminer]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_adminer_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, ansible_adminer]

    - local_action: stat path=/{{ playbook_dir }}/roles/geerlingguy.java
      register: geerlingguy_java_exists
      tags: [ansible, ansible_java]

    - name: install geerlingguy java from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.java
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, ansible_java]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_java_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, ansible_java]

    - local_action: stat path=/{{ playbook_dir }}/roles/geerlingguy.solr
      register: geerlingguy_solr_exists
      tags: [ansible, ansible_solr]

    - name: install geerlingguy solr from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.solr
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, ansible_solr]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_solr_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, ansible_solr]

    - local_action: stat path=/{{ playbook_dir }}/roles/geerlingguy.mysql
      register: geerlingguy_mysql_exists
      tags: [ansible, ansible_mysql]

    - name: install geerlingguy mysql from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.mysql
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, ansible_mysql]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_mysql_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, ansible_mysql]

    - local_action: stat path=/{{ playbook_dir }}/roles/geerlingguy.composer
      register: geerlingguy_composer_exists
      tags: [ansible, ansible_composer]

    - name: install geerlingguy composer from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.composer
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, ansible_composer]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_composer_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, ansible_composer]

    - local_action: stat path=/{{ playbook_dir }}/roles/geerlingguy.drush
      register: geerlingguy_drush_exists
      tags: [ansible, ansible_drush]

    - name: install geerlingguy drush from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.drush
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, ansible_drush]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_drush_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, ansible_drush]

    - local_action: stat path=/{{ playbook_dir }}/roles/geerlingguy.htpasswd
      register: geerlingguy_htpasswd_exists
      tags: [ansible, ansible_htpasswd]

    - name: install geerlingguy drush from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.htpasswd
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, ansible_htpasswd]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_htpasswd_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, ansible_htpasswd]

    - name: install geerlingguy certbot from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.certbot
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, certbot]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_htpasswd_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, certbot]

    - name: install geerlingguy xdebug from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.php-xdebug
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, xdebug]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_htpasswd_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, xdebug]

    - name: install geerlingguy php-versions from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.php-versions
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, xdebug]
      args:
        chdir: /{{ playbook_dir }}/
      when: geerlingguy_htpasswd_exists.stat.exists == False

    - debug: var=out.stdout_lines
      tags: [ansible, xdebug]

    - name: install geerlingguy geerlingguy.gitlab from galaxy
      command: ansible-galaxy install --roles-path roles geerlingguy.gitlab
      register: out
      retries: "{{ network_retries }}"
      delay: "{{ network_delay }}"
      until: out.rc == 0
      tags: [ansible, gitlab]
      args:
        chdir: /{{ playbook_dir }}/

    - debug: var=out.stdout_lines
      tags: [ansible, gitlab]

    - name: ./vagrant box update --insecure 
      shell: ./vagrant box update --insecure
      register: out
      ignore_errors: true
      retries: "{{ command_retries }}"
      delay: "{{ command_delay }}"
      until: out.rc == 0
      args:
        chdir: /{{ playbook_dir }}/vagrant/
      become: false

      tags: host_authoized_key_purge

    - debug: var=out.stdout_lines
      tags: host_authoized_key_purge

    - name: Recursively change ownership of a directory
      shell: chown -R {{baremetal_host_username}} /{{ playbook_dir }}
      register: out
      become: true
      ignore_errors: true
      tags: chown
      args:
        chdir: /{{ playbook_dir }}/

    - debug: var=out.stdout_lines

    - name: Create code directory
      become: yes
      file:
        path: /{{ playbook_dir }}/code
        state: directory
      tags: [code-directory]

    - name: Recursively change ownership of a directory
      shell: chown -R {{baremetal_host_username}} {{ playbook_dir }}/code
      register: out
      become: true
      ignore_errors: true
      args:
        chdir: /{{ playbook_dir }}/
      tags: [code-directory]

    - name: Generate an OpenSSL private key with the default values (4096 bits, RSA)
      community.crypto.openssl_privatekey:
        path: /etc/ssl/private/ansible.com.pem
      tags: [crypto]

    - name: Check state of Vagrant default machine
      command: ./vagrant status default --machine-readable
      args:
        chdir: /{{ playbook_dir }}/vagrant/
      register: vagrant_default_status
      retries: "{{ command_retries }}"
      delay: "{{ command_delay }}"
      until: vagrant_default_status.rc == 0
      changed_when: false
      become: false
      tags: vagrant-status-default

    - name: Extract Vagrant default machine state
      set_fact:
        vagrant_default_state: >-
          {{
            (vagrant_default_status.stdout
            | regex_findall('(?m)^\\d+,default,state,([^,\\n]+)')
            | last
            | default('unknown'))
          }}
      tags: vagrant-status-default

    - name: Start Vagrant default machine when not running
      command: ./vagrant up default
      args:
        chdir: /{{ playbook_dir }}/vagrant/
      register: vagrant_default_up
      retries: "{{ command_retries }}"
      delay: "{{ command_delay }}"
      until: vagrant_default_up.rc == 0
      when: vagrant_default_state != 'running'
      become: false
      tags: vagrant-up-default

    - name: Report Vagrant default machine state
      debug:
        msg: >-
          Vagrant machine 'default' state is '{{ vagrant_default_state }}'{% if vagrant_default_state != 'running' %}; started with './vagrant up default'.{% else %}; startup skipped.{% endif %}

