#################################################
# Host Configurations
#################################################
---
- hosts: host
  tasks:
    - name: Pwd
      shell: pwd
      register: out

    - debug: var=out.stdout_lines
      
    - name: Self ping
      shell: ping 127.0.0.1 -c 1
      register: out

    - debug: var=out.stdout_lines
